stages:
  - build
  - deploy

build-job:
  stage: build
  tags:
    - shell
  script:
    - echo "Building the application..."
    - ./mvnw clean package -DskipTests
  artifacts:
    paths:
      - target/*.jar
      - Dockerfile
      - docker-compose.yml
      - deploy.sh
    expire_in: 1 week
  only:
    - main

deploy-job:
  stage: deploy
  tags:
    - shell
  script:
    - echo "Deploying the application..."
    - chmod +x deploy.sh
    - ./deploy.sh
  environment:
    name: production
  only:
    - main
    